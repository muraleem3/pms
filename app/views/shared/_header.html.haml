.header
  .top-bar-one
    .top-bar-one-left
      = image_tag "logo6.jpg", :height => '55', :width => '50', :align => 'left', :class => "img-circle img-thumbnail", :style => "margin-left:3px;"
    .top-bar-one-middle
      
    .top-bar-one-right
      - if !current_user.present? 
        = link_to "Login", user_session_path, :data => {:method => :post}
        %span ||
        = link_to "REGISTER",new_user_registration_path
        %span || 
      - if current_user.present?
        = link_to "Log out", destroy_user_session_path, :data => {:method => :delete}
  .top-bar-two
    .top-bar-two-left
      = image_tag "logo6.jpg",:id =>"main-logo"
    .top-bar-two-right
      = link_to " Post Your Requirement",postrequirement_home_index_path, :class => 'btn btn-primary btn-cons'
      = link_to " List Your Property", listproperty_home_index_path, :class => 'btn btn-primary btn-cons'
      Location
      %select.selectpicker
        %option Hyderabad
        %option Chennai
        %option Mumbai

